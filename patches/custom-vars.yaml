# Set zuul debug mode until it stabilize
zuul_loglevel: DEBUG
nodepool_loglevel: DEBUG

# Set retry attempts to 2 in CI
zuul_default_retry_attempts: 2

# Enable using hypervisor-oci on the install-server
enable_insecure_slaves: true
