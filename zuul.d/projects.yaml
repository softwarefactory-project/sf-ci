---
- project:
    templates:
      - imported-sf-ci-jobs
    check:
      jobs:
        - linters
    gate:
      jobs:
        - linters
    experimental:
      jobs:
        - sf-rpm-build
        - sf-ci-openshift-integration:
            dependencies:
              - sf-rpm-build
