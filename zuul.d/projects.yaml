---
- project:
    check:
      jobs:
        - sf-rpm-build
        - sf-ci-rc-functional-allinone:
            dependencies:
              - sf-rpm-build
        - sf-ci-rc-upgrade-allinone:
            dependencies:
              - sf-rpm-build
