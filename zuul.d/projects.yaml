---
- project:
    # Temp trigger of the job
    check:
      jobs:
        - sf-ci-remote-gerrit
    # templates:
    #   - sf-ci-jobs
    # periodic:
    #   jobs:
    #     - sf-deploy-stable:
    #         branches: master
    #     - sf-deploy-stable-cr:
    #         branches: master
    # experimental:
    #   jobs:
    #     - sf-rpm-build
    #     - sf-ci-openshift-integration:
    #         dependencies:
    #           - sf-rpm-build
    #     - sf-ci-remote-gerrit:
    #         dependencies:
    #           - sf-rpm-build
