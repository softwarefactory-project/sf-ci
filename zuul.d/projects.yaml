---
# Will be reverted after sf-cores reviews
- project:
    templates:
      - sf-ci-jobs
    periodic:
      jobs:
        - sf-deploy-stable:
            branches: master
        - sf-deploy-stable-cr:
            branches: master
