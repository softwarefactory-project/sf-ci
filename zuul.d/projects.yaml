---
- project:
    templates:
      - sf-ci-jobs
    check:
        - sf-deploy-stable:
            branches: master
        - sf-ci-openshift-integration
    periodic:
      jobs:
        - sf-deploy-stable:
            branches: master
        - sf-deploy-stable-cr:
            branches: master
    experimental:
      jobs:
        - sf-rpm-build
        - sf-ci-openshift-integration:
            dependencies:
              - sf-rpm-build
