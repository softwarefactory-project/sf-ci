---
- project:
    templates:
      - sf-ci-jobs
    check:
      jobs:
        - linters:
            nodeset: fedora-latest-pod
    gate:
      jobs:
        - linters:
            nodeset: fedora-latest-pod
    experimental:
      jobs:
        - sf-rpm-build
        - sf-ci-openshift-integration:
            dependencies:
              - sf-rpm-build
