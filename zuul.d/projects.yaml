---
- project:
#    templates:
#      - sf-ci-jobs
    check:
      jobs:
        - sf-ci-functional-minimal
    gate:
      jobs:
        - linters
    periodic:
      jobs:
        - sf-deploy-stable:
            branches: master
        - sf-deploy-stable-cr:
            branches: master
    experimental:
      jobs:
        - sf-rpm-build
        - sf-ci-openshift-integration:
            dependencies:
              - sf-rpm-build
