---
- project:
    templates:
      - sf-ci-jobs
    check:
      jobs:
        - linters:
            nodeset: linters-pod
    gate:
      jobs:
        - linters:
            nodeset: linters-pod
    periodic:
      jobs:
        - sf-ci-functional-allinone
        - sf-deploy-stable:
            branches: master
        - sf-deploy-stable-cr:
            branches: master
    experimental:
      jobs:
        - sf-rpm-build
        - sf-ci-openshift-integration:
            dependencies:
              - sf-rpm-build
