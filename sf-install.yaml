# Install SF repos on hosts
- hosts: localhost
  tasks:
    # - include: tasks/install_sf_release.yaml
    - include: tasks/install_sf_ci_repo.yaml
    - include: tasks/yum_update.yaml

# Init SF on the main node
- hosts: localhost
  tasks:
    - include: tasks/install_sf_config.yaml
    - include: tasks/install_sf_arch.yaml
    - include: tasks/init_sf_config_playbooks.yaml

# Run sf_install tasks on nodes defined in the arch
- include: /var/lib/software-factory/ansible/sf_install.yml
