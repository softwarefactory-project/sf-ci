---
- hosts: master
  tasks:
    - name: Get worker ip in /etc/hosts files (configured by multinode parent)
      command: awk '/worker/ {print $1}' /etc/hosts
      register: host_worker_ip

    - set_fact:
        worker_ip: '{{ host_worker_ip.stdout }}'

- hosts: worker
  tasks:
    - name: Get master ip in /etc/hosts files (configured by multinode parent)
      command: awk '/master/ {print $1}' /etc/hosts
      register: host_master_ip

    - set_fact:
        master_ip: '{{ host_master_ip.stdout }}'

- hosts: all
  tasks:
    - name: Prepare instance
      include_role:
        name: '{{ item }}'
      loop:
        - prepare-test-env
        - install-ci-repository
        - install-sfconfig

- hosts: master
  vars:
    add_k1s_hypervisor: true
  tasks:
    - name: Configure sfconfig
      include_role:
        name: configure-sfconfig
      vars:
        add_component:
          - component: elasticsearch
          - component: repoxplorer
          - component: hound

    - name: Run sfconfig
      include_role:
        name: run-sfconfig

- hosts: worker
  tasks:
    - name: Fetch master CA pem file
      include_role:
        name: fetch-ca-pem
      vars:
        instance: master

    - name: Configure hostname and fqdn
      include_role:
        name: configure-fqdn
      vars:
        domain: worker.sftests.com

    - name: Configure sfconfig
      include_role:
        name: configure-sfconfig
      vars:
        sf_arch: tenant-minimal
        add_component:
          - component: gerrit
            insertafter: cauth
          - component: gitweb
          - component: elasticsearch
          - component: repoxplorer
          - component: hound
        sf_tenant_instance: true
        static_hostname_ip: '{{ master_ip }}'
        static_hostname_fqdn: sftests.com

    - name: Run sfconfig
      include_role:
        name: run-sfconfig

- hosts: master
  vars:
    add_k1s_hypervisor: true
  tasks:
    - name: Fetch master CA pem file
      include_role:
        name: fetch-ca-pem
      vars:
        instance: worker

    - name: Add gerrit connection in sfconfig.yaml
      include_role:
        name: configure-sfconfig
      vars:
        sf_main_instance: true
        static_hostname_ip: '{{ worker_ip }}'
        static_hostname_fqdn: worker.sftests.com

    - name: Run sfconfig to apply configuration
      include_role:
        name: run-sfconfig
      vars:
        sfconfig_args: --skip-install

    - name: Add resources in the config repo
      include_role:
        name: configure-resource

- hosts: worker
  tasks:
    - name: Run sfconfig to finalize configuration
      include_role:
        name: run-sfconfig
      vars:
        sfconfig_args: --skip-install

# this should not be needed but zuul didn't reconfigure when
# base, config-check and config-update are added in the last
# commit of the worker/config.
- hosts: master
  tasks:
    - name: Force scheduler reconfiguration
      command: zuul-scheduler full-reconfigure
      become: true

- hosts: worker
  tasks:
    - name: Add resources in tenant
      include_role:
        name: health-check/add_resources
      vars:
        fqdn: worker.sftests.com
        resource_file: ichiban.yaml
        tenant_deployment: True

    - name: Add zuul job in project
      include_role:
        name: health-check/create_zuul_project
      vars:
        fqdn: worker.sftests.com
        project: ichiban-compute

    - name: Validate project data are present on CodeSearch
      include_role:
        name: health-check/validate_project
      vars:
        service_uri: 'http://managesf.worker.sftests.com:6080/api/v1/search?q=TestHello&i=nope&files=&repos=ichiban-compute'
        service_until_condition: _result.json['Results']['ichiban-compute']['FilesWithMatch'] == 1

    - name: Validate project is present on RepoXplorer
      include_role:
        name: health-check/validate_project
      vars:
        service_uri: 'http://repoxplorer.worker.sftests.com:20002/api/v1/commits/commits?pid=ichiban-cloud'
