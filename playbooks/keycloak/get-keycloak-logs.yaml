---
- hosts: server
  tasks:
    - name: Fetch logs
      synchronize:
        src: /opt/jboss/keycloak/standalone/log/
        dest: "{{ zuul.executor.log_root }}/keycloak"
        mode: pull
      become: yes
