---
- hosts: install-server
  tasks:
    - name: Deploy stable sf instance
      include_role:
        name: '{{ item }}'
      loop:
        - install-mirrors
        - install-sfconfig
        - configure-sfconfig
        - run-sfconfig
