---
- hosts: install-server
  tasks:
    - name: Deploy stable sf instance
      include_role:
        name: '{{ item }}'
      loop:
        - install-mirror
        - update-system
        - install-sfconfig
        - configure-sfconfig
        - run-sfconfig
