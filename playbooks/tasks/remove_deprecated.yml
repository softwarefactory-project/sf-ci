- name: Remove deprecated services from the arch
  lineinfile:
    dest: /etc/software-factory/arch.yaml
    state: absent
    regexp: '{{ item }}'
  with_items:
    - zuul-server
    - zuul-merger
    - zuul-launcher
    - nodepool-launcher
    - nodepool-builder
  become: true
