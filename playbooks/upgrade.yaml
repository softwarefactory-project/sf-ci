---
- hosts: install-server
  roles:
    - install-sfinfo-repo
    - install-stable-repo
    - yum-update
    - check-bubblewrap
    - run-sfconfig
    - install-test-env
    - restore-ci-repo
    - install-sf-ci-repo
    - set-environment-facts
    - run-provisioner
    - run-sfconfig-upgrade
    - run-checker
    - run-health-check
    - run-functional-tests
