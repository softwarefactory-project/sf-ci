---
- hosts: install-server
  roles:
    - prepare-test-env
    - install-mirrors
    - update-system
    - check-bubblewrap
    - install-sfconfig
    - configure-sfconfig
    - set-hostname
    - run-sfconfig
    - setup-test-env
    - restore-ci-repo
    - role: install-mirrors
      update_mirrors: True
    - run-provisioner
    - install-sfconfig
    - role: run-sfconfig
      sfconfig_args: --upgrade
    - run-checker
    - run-health-check
    - run-functional-tests
