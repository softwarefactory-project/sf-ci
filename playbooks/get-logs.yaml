---
- hosts: install-server:master:slave
  tasks:
    - name: Collect artifacts
      include_role:
        name: '{{ item }}'
      loop:
        - fetch-instance-status
        - finish-test
        - fetch-artifacts
