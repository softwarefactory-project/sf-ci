---
- hosts: install-server
  tasks:
    - name: Collect artifacts
      include_role:
        name: '{{ item }}'
      loop:
        - fetch-instance-status
        - finish-test
        - fetch-artifacts
