---
- hosts: zuul-server
  remote_user: root
  tasks:
    - name: Check if the project is defined in layout.yaml
      command: "grep -r {{ project_name }} /etc/zuul"
      register: zuul_created
      ignore_errors: yes
      no_log: true
