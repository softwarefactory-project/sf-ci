- hosts: localhost
  tasks:
    - name: Clean previous /var/lib/software-factory/backup directory
      file:
        path: /var/lib/software-factory/backup
        state: absent
    - name: Run sfconfig --recover
      command: sfconfig.py --recover ./sf_backup.tar.gz
